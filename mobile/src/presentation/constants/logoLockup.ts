// Shared lockup positioning constants for splash/login continuity.
// Negative = move up from the screen center.
export const LOCKUP_LIFT_FROM_CENTER = -230

// Spinner sits under the centered lockup.
export const SPINNER_OFFSET_FROM_CENTER = 64

// Login form needs to sit below the absolute lockup.
export const LOGIN_FORM_SHIFT_DOWN = 80
